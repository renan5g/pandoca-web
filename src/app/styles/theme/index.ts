import { colors } from './colors';
import { zIndices } from './zIndices';
import { fonts } from './fonts';
import { spaces, align } from './utils';

export const myTheme = {
  colors,
  zIndices,
  fonts,
  spaces,
  align,
};
